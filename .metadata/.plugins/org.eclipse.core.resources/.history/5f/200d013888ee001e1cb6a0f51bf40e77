package task04;

import task03.MyException;

public class Task04 {

     //사용자로부터 숫자를 입력받아, 입력받은 숫자가 3의 배수인 경우 "Fizz", 
	 //5의 배수인 경우 "Buzz", 
	 //3과 5의 공배수인 경우 "FizzBuzz",
	 //그 외의 경우에는 입력받은 숫자를 출력하는 프로그램을 작성하세요. 
	 //사용자가 문자열을 입력할 경우,
	 //"숫자가 아닌 문자열이 입력되었습니다"라는 예외 메시지를 출력하세요.

		//필드  
		int  number;
		String msg;
			
		void trans(int  number) throws MyException{
			 try {
				 if(Integer.valueOf(number)) {
					 if((number % 3==0)&&(number % 5==0)) {
						 
						 System.out.println("FizzBuzz"); 
					 
					 }else if(number % 3==0) {
					 
						 System.out.println("Fizz");
						 
					 }else if(number % 5==0) {
						 
						 System.out.println("Buzz");
						 
					 }else{
						
						 System.out.println(number);
						 
					 }
		}else {
			
				throw new MyException("숫자가 아닌 문자열이 입력되었습니다");
				
		}

	}

}Integer numInt2 = Integer.valueOf(munja);
